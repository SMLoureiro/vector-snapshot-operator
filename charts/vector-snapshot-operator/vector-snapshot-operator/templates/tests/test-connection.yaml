apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vector-snapshot-operator.fullname" . }}-test-connection"
  labels:
    {{- include "vector-snapshot-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vector-snapshot-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
